# 一起做实验

先阅读 README 的“当前研究方向”。项目仍在方案验证阶段，功能范围、交互方式和用户自定义语义范围还在讨论。

1. 从 `main` 创建自己的分支，通过 Pull Request 合并，说明改变了哪个假设或行为。
2. 提交前运行 `python3 -m unittest -v`；使用合成样例验证入口，测试不要调用付费 API。
3. 筛选实验保存参数、模型版本、原始判断、删除候选和上下文，关注误删与净开销。
4. 只提交代码、通用文档及标为合成的样例。录音放 `recordings/`，结果放 `runs/`，私有资料放 `local/`，模型放 `models/`。
5. 各自用环境变量配置密钥。上传前检查 `git diff --cached`，不要用 `git add -f` 绕过私有目录的忽略规则。

当前父子切片实验与第一轮基线分别保留。新增实验应作为可对照的独立变化，不要提前把固定业务分类当成用户必须采用的语义范围。

本仓库目前为私有协作仓库，开源许可证与公开发布范围待共同决定。
