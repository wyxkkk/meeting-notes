# OBS：通用会议录音设置（Mac）

目标是录到电脑播放的声音和自己的麦克风，供会后处理。无需接入会议软件 API。

优先先验证一条混合音轨能录到双方声音。多音轨分配是可选配置，必须重新打开设置并通过短录音确认保存成功；只看到多条音轨，不代表它们已经分离。

## 一次性设置

1. 打开 OBS。自动配置向导选择“仅用于录制”；若不使用向导，直接进入设置。
2. 按 OBS 提示授予系统音频/屏幕录制与麦克风权限。录音方案不需要摄像头权限。
3. 新建场景“会议录音”。
4. 添加“macOS 音频采集 / macOS Audio Capture”，选择所有系统音频。可以选择单一应用来排除其他应用声音，但这仍是操作系统采集，无需会议平台 API。
5. 使用全局“麦克风/辅助音频”，选实际使用的麦克风；或者改用“音频输入采集 / Audio Input Capture”。
6. 同一麦克风只保留一路输入。若已启用全局麦克风，不需要再添加一个相同来源。
7. 设置 → 输出 → 输出模式选择“高级”。录制格式选 MKV，启用音轨 1、2、3。设置一个容易找到的录制目录。
8. 混音器 → 高级音频属性：系统音频勾选 1、2；麦克风勾选 1、3；两路的音频监听均保持关闭。
9. 画面可以保持空白；第一版程序会从录屏容器提取音频，不依赖图像内容。

macOS 13+ 和 OBS 30+ 可使用上述系统音频采集方式。菜单翻译可能随版本变化。

## 首次 30–60 秒验证

1. 戴上平时开会的耳机，使用平时的麦克风。
2. 开始录制。
3. 播放一小段英文语音；自己接着说一句英文和一句中文。
4. 确认混音器里系统声音和麦克风的电平都分别有变化。
5. 停止录制，回放第 1 条音轨，确认两边声音都有且没有明显回声。
6. 把文件交给程序的 `inspect` 和 `process` 命令。

没有远端声音通常是系统音频采集权限或音源选择的问题；没有自己的声音通常是麦克风选择或权限的问题。使用外放时麦克风可能再录到远端声音，耳机可以减少这类重复。

OBS 多音轨文件在普通播放器中一般只播放其中一条；不能据此认为其他音轨没有录到。程序 `inspect` 会列出所有音轨。

## 参考

- [macOS 音频采集](https://obsproject.com/kb/macos-desktop-audio-capture-guide)
- [权限说明](https://obsproject.com/kb/macos-permissions-guide)
- [多音轨录制](https://obsproject.com/kb/advanced-recording-guide-and-multi-track-audio)
