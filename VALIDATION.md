# 验证记录

验证覆盖命令行原型和筛选实验，不代表已达到产品可用性或真实会议转写准确率标准。

## 2026-09-20 原型验证

- 环境：Apple M5、24 GB、macOS 26.6.2；OBS 32.2.2、FFmpeg 9.0.2、whisper.cpp 1.9.4。
- large-v3-turbo-q5_0 模型已下载并核对 SHA-256，见 README。
- 约 21.74 秒合成英文语音成功转写为 5 段，Whisper 内部计时约 2.04 秒；单一样本不是性能基准。
- 同一合成语音的 Jev 调用保留全部内容，1 次请求、1,033 输入 token。
- 合成三音轨 MKV 选取索引 1、2 后正确导出两种来源标签，没有转写索引 0；不验证真实声音分离。
- 8 项初始单元测试通过：否定/取消原文、音轨来源、上下文补回、服务失败保留、显式疑点和导出。

## 同一真实录音的两次筛选实验

原始录音和转写未包含在仓库中。这里只保留汇总指标，不包含会议正文。

| 指标 | 整块＋五类判断 | 父块背景＋子片段单问题 |
|---|---:|---:|
| 输入子片段 | 1,249 | 1,249 |
| 输入正文字符 | 7,487 | 7,487 |
| HTTP 请求 | 29 | 29 |
| 独立判断 | 145 | 1,249 |
| Jev 输入 token | 121,416 | 178,372 |
| 删除子片段 | 0 | 0 |
| 正文减少率 | 0% | 0% |

第二次实验使用 60 秒父块、前后各 15 秒上下文，逐原始片段判断纯闲聊，P≥0.90 才删除。全部请求成功，筛选前后文本、时间戳和 ID 一致。用同一批概率计算，0.70 阈值会选出 4 个片段、17 个字符；没有据此修改正式输出。

两轮同时改变了问题和分段等条件，不能单独归因于上下文重叠。当前数据没有证明节省 token 或费用，15 秒重叠也没有“无重叠”对照实验支持。

## 仓库迁移验证

保留原有 8 项测试，增加跨分钟片段分配、其他工作目录运行、已有输出不被覆盖的 3 项测试，11 项全部通过。合成文本导出和 Jev 请求预览均通过离线验证，未重新发送真实会议内容。

## 尚未验证

- 转写相对于原始音频的完整准确率，尤其是姓名、英文术语、否定词和数字。
- 多人音轨的发言人归属、回声、交叠讲话及设备切换。
- 更多闲聊和边界样本上的误删率，以及下游纪要质量。
- 跨平台安装体验与自动化纪要生成。
